/**
 * İyzico resmi Node SDK’si npm’de `iyzipay` paket adıyla yayınlanır.
 * Uygulama katmanı yalnızca bu modülü kullanır; `iyzipay` doğrudan import edilmez.
 */
import Iyzico from 'iyzipay';

export default Iyzico;
